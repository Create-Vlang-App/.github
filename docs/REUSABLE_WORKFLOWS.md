# Reusable workflows

Evaluation (org issue #6): prefer repo-local workflows for V/`setup-v` until a shared composite action is justified.
Reusable candidates later: markdownlint, draft-skip pattern, setup-v pin.
